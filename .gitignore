*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
*.apk
*.aab
.kotlin/
gradle.properties

# 运行日志和性能监控文件
*.log
*.txt
!gradle/wrapper/gradle-wrapper.properties
perf_monitor/

# 签名文件（绝不能提交）
*.jks
*.keystore
keystore.properties
/app/release/

# Windows 保留设备名（bash 误用 2>nul 会创建名为 nul 的文件，cmd.exe 里是空设备）
# 避免在 bash 环境误用 Windows 重定向语法导致脏文件混入
nul
CON
PRN
AUX
